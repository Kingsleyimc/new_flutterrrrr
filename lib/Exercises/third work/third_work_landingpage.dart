// ignore_for_file: prefer_const_constructors

import 'package:flutter/material.dart';

class ThirdWorkLandingpage extends StatelessWidget {
  const ThirdWorkLandingpage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('third work landing page'),
      ),
    );
  }
}